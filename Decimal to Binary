#include<stdio.h>
#include<stdlib.h>

int main()
{
  int n,rem=0,i=0,a[100];
  printf("enter a num:");
  scanf("%d",&n);

  while(n!=0)
    {
      rem=n%2;
      n=n/2;
      a[i]=rem;
      i++;
    }

  for(int j=i-1;j>=0;j--)
    {
      printf("%d ",a[j]);
    }
}
